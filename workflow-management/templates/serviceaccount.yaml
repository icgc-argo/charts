{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "workflow-management.serviceAccountName" . }}
  labels:
{{ include "workflow-management.labels" . | nindent 4 }}
{{- end -}}
