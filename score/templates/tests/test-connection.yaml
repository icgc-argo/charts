apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "score.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "score.name" . }}
    helm.sh/chart: {{ include "score.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "score.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
