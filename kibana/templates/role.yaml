apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "kibana.fullname" . }}
rules:
- apiGroups:
  - policy
  resourceNames:
  - {{ include "kibana.fullname" . }}
  resources:
  - podsecuritypolicies
  verbs:
  - use
