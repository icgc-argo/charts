apiVersion: v1
kind: Service
metadata:
  name: {{ include "rdpc-status.fullname" . }}
  labels:
    {{- include "rdpc-status.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rdpc-status.selectorLabels" . | nindent 4 }}
