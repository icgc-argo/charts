apiVersion: v1
kind: Service
metadata:
  name: {{ include "workflow-traffic.fullname" . }}
  labels:
    {{- include "workflow-traffic.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "workflow-traffic.selectorLabels" . | nindent 4 }}
