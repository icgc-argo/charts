apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "workflow-traffic.fullname" . }}-test-connection"
  labels:
{{ include "workflow-traffic.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "workflow-traffic.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
