apiVersion: v1
kind: Service
metadata:
  name: {{ include "rdpc-kube-mutating-webhook.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "rdpc-kube-mutating-webhook.name" . }}
    helm.sh/chart: {{ include "rdpc-kube-mutating-webhook.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "rdpc-kube-mutating-webhook.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
